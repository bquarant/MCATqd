<h1>Question Database</h1>
<br>
<table class="table table-hover table-striped">
  <tr>
    <th>Question Stem</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @questions.each do |question| %>
  <tr>
    <td><%= question.content %></td>
	<!-- TODO: Display only if current_user has a paid subscription  -->
    <td><%= link_to 'Show', question %></td>
	<!-- TODO: Display only if current_user is an administrator -->
    <td><%= link_to 'Edit', edit_question_path(question) %></td>
    <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<!-- TODO: Only show if user is an administrator -->
<center><%= link_to 'Create New Question', new_question_path, :class=>"btn btn-large btn-primary" %></center>
